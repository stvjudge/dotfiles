return {
    cmd = { "just-lsp" },
    filetypes = { "Justfile", "just" },
    settings = {
        -- bashIde = {
        --     -- Disable shellcheck in bashls. Conflicts with linter settings.
        --     shellcheckPath = "",
        -- },
    },
}
